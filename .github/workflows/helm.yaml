name: Helm Package & Push

on:
  push:
    tags:
      - "*"

jobs:
  helm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      - name: Verify Chart version matches tag
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/}
          CHART_VERSION=$(grep '^version:' Chart.yaml | awk '{print $2}' | tr -d '"')
          echo "Tag version: $TAG_VERSION"
          echo "Chart version: $CHART_VERSION"
          if [ "$TAG_VERSION" != "$CHART_VERSION" ]; then
            echo "Error: Tag version ($TAG_VERSION) does not match Chart.yaml version ($CHART_VERSION)"
            exit 1
          fi
          echo "Version check passed: $TAG_VERSION"
      - name: Helm lint & package & push
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          helm version
          HELM_EXPERIMENTAL_OCI=1 helm registry login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}
          HELM_EXPERIMENTAL_OCI=1 helm registry login ${REGISTRY} -u ${ARTIFACTORY_USERNAME} -p ${ARTIFACTORY_PASSWORD}
          helm lint .
          helm package .
          HELM_EXPERIMENTAL_OCI=1 helm push $(ls *.tgz| head -1) oci://ghcr.io/caas-team/charts
          HELM_EXPERIMENTAL_OCI=1 helm push $(ls *.tgz| head -1) oci://${REGISTRY}/${REPO}
        env:
          ARTIFACTORY_USERNAME: ${{secrets.ARTIFACTORY_USERNAME}}
          ARTIFACTORY_PASSWORD: ${{secrets.ARTIFACTORY_PASSWORD}}
          REGISTRY: artifactory.devops.telekom.de
          REPO: caas-imagetransfers-helmoci-local
